pipeline {
    agent any

    stages {
        stage('num') {
            steps {
               echo " Enter a two value of range "
               read a
               read b
               while '(' a -le b ')'
               do
                     echo $a
                     a=((a+1))
               done
            }
        }
    }
}
